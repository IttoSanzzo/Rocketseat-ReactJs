import createComponent from "../../../libs/createComponent";
import styles from "./_app.module.css";

export const Container = createComponent.div(styles.container);
export const Header = createComponent.header(styles.header);
